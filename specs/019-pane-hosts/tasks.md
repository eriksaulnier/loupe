---

description: "Task list for detecting and opening a pane in Herdr or Orca, through one host seam, one direction rule and one step-keyed refusal"
---

# Tasks: Pane hosts

**Input**: Design documents from `specs/019-pane-hosts/`

**Prerequisites**: [plan.md](plan.md), [research.md](research.md), [quickstart.md](quickstart.md), [spec.md](spec.md), `.specify/memory/constitution.md`

**Tests**: Behavior changes in two packages and one skill, so every phase that touches code opens with the failing test the constitution's Development Workflow requires. Goldens are regenerated only after T009 rewrites `handoffHelp`, and the regeneration is followed by reading the diff.

**Story labels**: No phase here is organized by a single user story from `spec.md`; the design groups by file instead (`internal/pane`'s three files, then the CLI, then documents), and Orca (US1), the sandbox rerun rule (US2) and the Herdr regression check (US3) each land across several of those phases. Tasks therefore carry no `[Story]` tag.

## Format: `[ID] [P?] Description`

- **[P]**: Can run in parallel (different files, no dependency on an incomplete task in the same phase)
- Include exact file paths and function or test names in descriptions

## Phase 1: `internal/pane` seam (`pane.go` + `pane_test.go`)

- [x] T001 Rewrite `internal/pane/pane_test.go` first, failing, to keep only the host-independent shared cases: a table asserting `Direction(120, true)` is `right` and `Direction(119, true)` and `Direction(0, false)` are `down` (amended 2026-09-21: the table is now over `direction(hostWidth, hostOK, tty)`, with tty 120 `right`, 119 `down`, unreadable and absent `right`, and a host width overriding the tty); and a case asserting `failed("herdr", "probe", "boom", "fix")` returns a `*refusal.Error` with `Code: refusal.PaneFailed`, `Message: "herdr probe: boom"`, `Fix: "fix"` and `Details: map[string]any{"host": "herdr", "step": "probe"}`. Delete `TestOpenSendsLayoutSplitAndRun`, `TestOpenSplitsDownBelowTheWidthThreshold`, `TestOpenRefusesAndStopsAtTheFailedStep`, `TestDetectNeedsEnvPaneAndBinary` and `TestHerdrMessageReadsTheErrorObject`; they move into `herdr_test.go` in Phase 2, renamed.
- [x] T002 Rewrite `internal/pane/pane.go` down to the shared seam: `type Host interface { Name() string; Open(ctx context.Context, req Request) (Opened, error) }`; `Request{Command, Direction, Fix string}` (amended 2026-09-21: `Direction` is replaced by `TTYWidth func() (int, bool)` and the host decides; drop `DataRoot` — neither host reads it once `LOUPE_HOME` travels inside `Command`); `Opened{PaneID, Direction string}` unchanged; `Direction(width int, ok bool) string` returning `right` when `ok` and `width >= wideEnough` (120), else `down` (amended 2026-09-21: now `direction(hostWidth int, hostOK bool, tty func() (int, bool)) string`, host width first, then tty, unknown `right`); `TTYWidth() (int, bool)` opening `/dev/tty` and calling `term.GetSize` on its fd, closing it, any error `(0, false)`; `Runner func(ctx context.Context, path string, args ...string) (stdout, stderr []byte, err error)` and the shared `execRunner` implementing it; `failed(host, step, message, fix string) error` building the `*refusal.Error` T001 pins; `lookPath(getenv func(string) string, name string) (string, bool)` factored out of today's `Detect`'s `PATH` walk. Leave `Detect`, `Name` and the `Herdr` type out of this file for now. Confirm with `go test ./internal/pane/ -run 'Direction|Failed'`.

## Phase 2: `herdr.go` + `herdr_test.go`

- [x] T003 Write `internal/pane/herdr_test.go` first, failing: a detection case testing `detectHerdr` directly (not `Detect`, which does not exist yet) against the same `HERDR_ENV`, `HERDR_PANE_ID` and `PATH` cases `pane_test.go` dropped in T001; `TestHerdrMessageReadsTheErrorObject` moved unchanged; `TestOpenSendsLayoutSplitAndRun` renamed `TestOpenSendsProbeSplitAndRun` and rewritten to drop the split's `--env LOUPE_HOME=` argument (amended 2026-09-21: the layout width is read again, and `TestOpenTakesTheDirectionFromTheLayoutWidth` covers 119 → `down` and 120 → `right` over a tty width, and a pane without a width falling back to the tty), asserting calls `pane layout --pane <id>`, `pane split --pane <id> --direction <direction> --focus` (no `--env`) and `pane run <new id> <req.Command>`; `TestOpenSplitsDownBelowTheWidthThreshold` deleted (amended 2026-09-21: replaced by `TestOpenTakesTheDirectionFromTheLayoutWidth`); `TestOpenRefusesAndStopsAtTheFailedStep` with its step keys renamed `"layout"` → `"probe"` throughout, including the `pane_test.go:89` case, asserting the message `herdr probe: pane w1:p1 not found`.
- [x] T004 Add `internal/pane/herdr.go`: `detectHerdr(getenv func(string) string) (Herdr, bool)` keeping FR-001's conditions (`HERDR_ENV` is `1`, `HERDR_PANE_ID` non-empty, `herdr` found via `lookPath`); `type Herdr struct` with `Name() string { return "herdr" }`; `Open(ctx context.Context, req Request) (Opened, error)` with steps `probe` (`pane layout --pane <id>`, checked for a zero exit, a parseable result listing the agent's pane, and, amended 2026-09-21, that pane's `rect.width` read into the shared direction rule), `split` (`pane split --pane <id> --direction <direction> --focus`, no `--env`, requiring `result.pane.pane_id`) and `run` (`pane run <new id> <req.Command>`), each failure going through `failed("herdr", step, herdrMessage(stdout, stderr, err), req.Fix)`; move `herdrMessage` here, taking `(stderr []byte, err error)` (amended 2026-09-21: its unused stdout parameter is dropped), still reading `error.message` from stderr JSON else trimmed stderr. Confirm with `go test ./internal/pane/ -run Herdr`.

## Phase 3: `orca.go` + `orca_test.go`

- [x] T005 Write `internal/pane/orca_test.go` first, failing: a detection case for `detectOrca` needing `ORCA_TERMINAL_HANDLE` non-empty and an executable `orca` on `PATH`, and reading no other variable, in particular not `TERM_PROGRAM`; `TestOpenSendsShowSplitAndSwitch` with a canned `show` reply (`{"ok":true}`), the recorded split reply `{"ok":true,"result":{"split":{"handle":"term_28c2bdd2-…"}}}` (research.md, "Live probe") mapping `right` → `--direction vertical` and `down` → `--direction horizontal`, and a `switch` call, asserting `Opened{PaneID: "term_28c2bdd2-…", Direction: req.Direction}` (amended 2026-09-21: the direction comes from the tty alone, 120 `right`, 119 `down`, unreadable and absent `right`; `TestOpenRefusesWithTheMessageOfANotOKReplyOnExitZero` covers `ok:false` with an `error.message` on exit 0); `TestOpenRefusesOnAStaleHandle` with the stale-handle shape `{"ok":false,"error":{"code":"terminal_handle_stale","message":"…"}}` on stdout, exit 1, and the handshake line on stderr, asserting the message `orca probe: <error.message>` and exactly one call; `TestOpenRefusesOnASplitWithoutAHandle` (a split reply with `ok:true` but no `result.split.handle`) asserting `orca split: the result has no handle` and no `switch` call; `TestOpenRefusesOnASwitchFailureAfterASplit` asserting `orca switch: …` after `show` and `split` both succeeded; `TestOrcaMessagePrefersStdoutErrorThenNonHandshakeStderr` for `orcaMessage`, covering the stdout JSON case, the stderr-minus-handshake fallback, and the bare exit-error case.
- [x] T006 Add `internal/pane/orca.go`: `detectOrca(getenv func(string) string) (Orca, bool)` needing `ORCA_TERMINAL_HANDLE` non-empty and an executable `orca` on `PATH` via `lookPath`, reading no other variable; `type Orca struct` with `Name() string { return "orca" }`; `Open(ctx context.Context, req Request) (Opened, error)` with steps `probe` (`terminal show --terminal <handle> --json`, requires `ok:true`), `split` (`terminal split --terminal <handle> --direction horizontal|vertical --command <req.Command> --json`, mapping `right`→`vertical` and `down`→`horizontal` from `direction(0, false, req.TTYWidth)`, requires `ok:true` and `result.split.handle`; a missing handle refuses at `split` and calls no `switch`) and `switch` (`terminal switch --terminal <new handle> --json`); `orcaMessage(stdout, stderr []byte, err error) string` reading `error.message` from stdout JSON, else the last stderr line not starting `[relay-connect]`, else the exit error. In `internal/pane/pane.go`, add `Detect(getenv func(string) string) (Host, bool)` trying `detectHerdr` then `detectOrca` and returning the first match; restore into `pane_test.go` the shared cases T001 held back — detection order with both environments set (only Herdr runs) and a host whose conditions are met only in part being skipped, not refused (FR-003). Confirm with `go test ./internal/pane/`.

## Phase 4: CLI handoff, `Deps`, `cmd` wiring and goldens

- [x] T007 In `internal/cli/root.go`, add `TTYWidth func() (int, bool)` to `Deps` beside `TermWidth`; nil means unreadable, so `deps.TTYWidth` is called nil-safe and a test that does not set it hands off `down` (amended 2026-09-21: the host's width, else `right`).
- [x] T008 Rewrite `internal/cli/handoff_test.go`'s fakes and cases first, failing: `fakeHerdrBin` drops the layout width from its canned `layout` reply (amended 2026-09-21: it reports 150, and `fakeHerdrBinWithRect` sets another), and its `split` case stops expecting `--env`; add `fakeOrcaBin(t, failAt string) (bin, log string)` mirroring `fakeHerdrBin`'s argv-logging shape, printing the handshake line `[relay-connect] Handshake OK at version=0.1.0+…` on stderr on every call, and at `failAt` printing Orca's error shape on stdout and exiting 1; add `orcaEnv(handle, bin string) map[string]string` setting `ORCA_TERMINAL_HANDLE` and `PATH`. New cases: `TestHandoffOpensReviewInAnOrcaSplit` asserting the `show`, `split`, `switch` calls in order and a result with `host: orca` and `paneId` as the new handle; `TestHandoffRefusesOnAStaleOrcaHandle` asserting `pane-failed` with `details.host: orca`, `details.step: probe`, and exactly one call; `TestHandoffUsesHerdrWhenBothHostsAreDetected` setting both environments and asserting only `herdr` runs; `TestHandoffDirection` table-driven over widths 120, 119 and an unset `deps.TTYWidth`, asserting `right`, `down`, `down` on both hosts (amended 2026-09-21: Herdr's layout width 120 and 119 over a tty width, a missing layout width falling back to the tty, and on Orca the tty with unreadable and unset as `right`; `TestShellQuoteMakesOneLiteralWord` pins FR-008's quoting against literals); `TestHandoffRefusesNoPaneHostNamingBothHosts` asserting the message names `HERDR_ENV=1, HERDR_PANE_ID and herdr on PATH` and `ORCA_TERMINAL_HANDLE and orca on PATH`, and that neither binary runs. Extend `TestHandoffRefusesPaneFailedAndStops` to assert `details.host` and `details.step` on both a Herdr and an Orca failure. Update `TestHandoffOpensReviewInAHerdrSplit`'s expected calls to drop `--env LOUPE_HOME=` from the `split` call.
- [x] T009 Rewrite `internal/cli/handoff.go`: after the `review-open` check, call `pane.Detect(deps.Getenv)`; on no host, refuse `no-pane-host` with FR-004's message naming both hosts' conditions; keep building the pane's command once through `reviewCommand` (still `LOUPE_HOME='<root>' '<exe>' review '<ref>' && exit`); compute `direction := pane.Direction(deps.TTYWidth())`, treating a nil `deps.TTYWidth` as `(0, false)`; call `host.Open(cmd.Context(), pane.Request{Command: command, Direction: direction, Fix: fix})` (amended 2026-09-21: `handoff` passes `TTYWidth: deps.TTYWidth` and reports `opened.Direction`); report `host.Name()` as the result's `host` field in place of the old `pane.Name` constant. Rewrite `handoffHelp` host-neutral per FR-014: one line per host's detection conditions, the detection order, the direction rule (amended 2026-09-21: the host's report of the agent pane's width, else the agent's terminal, else `right`), both refusals (`no-pane-host`, `pane-failed`) naming `pane-failed`'s `details.step`, and the result naming `host` as `herdr` or `orca`. Confirm with `go test ./internal/cli/ -run Handoff`.
- [x] T010 [P] In `cmd/loupe/main.go` and `cmd/loupe-demo/main.go`, set `TTYWidth: pane.TTYWidth` in each `Deps` literal; leave `cmd/loupe-demo/main_test.go`'s `Deps` without it, so it stays nil.
- [x] T011 Run `go test ./internal/cli/ -update`, then `git diff testdata/golden/cli`. Expect only `handoff-help.80.txt` and `handoff-help.100.txt` to move, matching T009's rewritten `handoffHelp`. Read the diff.

## Phase 5: Documents

- [x] T012 [P] Amend the `loupe handoff` section of `specs/001-loupe-v1/contracts/cli.md` (around line 127) and the `pane-failed` row of its refusal table (line 66): name both hosts and their detection, the detection order, that the direction comes from the host's report of the agent pane's width, else the agent's terminal, else `right` (amended 2026-09-21), the `host` values `herdr` and `orca`, and that `pane-failed` carries `details.host` and `details.step`, with `probe` the step that opens nothing on every host (FR-016).
- [x] T013 [P] In `internal/cli/plugin_test.go`, write the failing assertions first: extend `TestPluginSkillNamesNoHerdrCommand` (line 147) to also strip and reject `orca`, since the skill's sandbox line no longer names either host's command; rewrite the sandbox-rule literal `TestPluginSkillProhibitions` pins (line 182) to say `loupe handoff` needs "the terminal host's socket" and to rerun only when `error.details.step` is `probe`, replacing the match on `error.message` starting `herdr pane layout`.
- [x] T014 Rewrite sections 6 and 7 of `plugin/skills/human-review/SKILL.md`'s sandbox rule (FR-017, FR-018) to match T013: `loupe handoff` needs the terminal host's socket; rerun it outside the sandbox only when `error.details.step` is `probe`, because a later step can already have opened a pane; name no `herdr` or `orca` command anywhere in the skill. Confirm with `go test ./internal/cli/ -run Plugin`.
- [x] T015 [P] Update `README.md`: rename "Handing off in a Herdr pane" (line 72) to "Handing off in a terminal pane" and describe both Herdr and Orca; change the Codex sandbox note (line 70) to say "the terminal host's socket"; change the Commands table's `handoff` row (line 92) to say "new terminal pane"; extend the blanket-allow-rule warning (line 81) to also name `orca terminal split` (FR-019).
- [x] T016 [P] Update `AGENTS.md`'s `internal/pane` layout row (line 20) to describe the package as opening review in a new Herdr or Orca pane and as the only package that runs either; update `CONTRIBUTING.md`'s demo hand-off line (line 54) to say "inside Herdr or Orca" in place of "inside Herdr" (FR-020).
- [x] T017 [P] Widen the host guard in `scripts/check-tests.sh`'s `herdr=` block: grep `herdr|orca` (case-insensitive) with today's exclusions (`internal/pane/*`, `internal/cli/handoff.go`, `*_test.go`) and change its violation text to say "Herdr or Orca" (FR-015).
- [x] T018 [P] Add "superseded on 2026-09-21 by `specs/019-pane-hosts`" notes to `specs/006-agent-plugins/spec.md` FR-002, FR-003 and FR-004, and amendment notes to FR-009's `host` value list and to FR-006, FR-010, FR-014 and FR-017, in the form 006 already used to note 003's supersession (FR-021).
- [x] T019 Update `specs/001-loupe-v1/validation.md`: record the new `internal/pane` and `internal/cli/handoff_test.go` tests; add FR-023's Unverified rows — whether the controlling terminal is readable from Claude Code's Bash tool (verified unreadable on 2026-09-21) and from Codex outside its sandbox, whether an Orca pane closes when review itself exits with `q`, a `down` split in Orca, and a Herdr handoff after `LOUPE_HOME` moved into the command line — noting that the pane-closing-on-shell-exit fact is already observed and dated 2026-09-21 (research.md, "Live probe"). Leave space for T021 to fill in FR-022's live-check outcomes, or to move any step it could not run onto the Unverified list instead.

## Phase 6: Check

- [x] T020 Run `mise run check` (`go vet ./...`, `golangci-lint run`, `actionlint`, `scripts/check-tests.sh`, `go test ./...`). Fix anything it surfaces from Phases 1 through 5 and rerun until green (SC-005).

## Phase 7: Live checks

- [x] T021 Record, in `specs/001-loupe-v1/validation.md` (T019), the results the orchestrator reports from quickstart.md's "Live, in Orca" steps, run by the human, not this agent: from an agent terminal at least 120 columns wide, `LOUPE_DEMO_HOME=.demo mise run demo -- handoff 'acme/widgets#42' --json` returning `host: orca`, `direction: right`, and review opening in the split with focus; pressing `q` in review closing the pane; handing off a published run so `loupe-demo review` refuses in the pane, leaving it open. Any step not run stays on the Unverified list per FR-022 and FR-023; this task records outcomes only and runs no host command itself.

## Phase 8: Focus gate (added 2026-09-22, FR-024)

- [x] T022 Write the failing tests first in `internal/pane/orca_test.go`: `TestDetectOrcaNeedsHandleAndBinary` also reads `ORCA_TAB_ID` and detects without it; `TestOpenSendsShowSplitListAndSwitchWhenTheAgentTabIsActive` (renamed from `TestOpenSendsShowSplitAndSwitch`), `TestOpenSkipsSwitchWhenTheAgentTabIsInTheBackground`, `TestOpenSwitchesWithoutListingWhenTheTabIsUnknown` and `TestOpenRefusesAtLayout` (tab in no group, unreadable listing, failed listing); `herdr_test.go` asserts `Focused: true`.
- [x] T023 In `internal/cli/handoff_test.go`, set `ORCA_TAB_ID` in `orcaEnv`, answer `list` in `fakeOrcaBin` through `fakeOrcaBinWithActiveTab`, assert `focused: true` for Herdr and a foreground Orca tab, and add `TestHandoffLeavesTheViewAloneFromABackgroundOrcaTab` for `focused: false`, the "without focus" line and no `switch` call.
- [x] T024 Add `Opened.Focused`, set it on Herdr, read `ORCA_TAB_ID` in `detectOrca`, add Orca's `layout` step and gate `switch` on it in `internal/pane/orca.go`; add `focused` to the `handoff` result and "without focus" to its plain line, and amend its help. Regenerate goldens with `go test ./internal/cli/ -update`; only `handoff-help.*` moved.
- [x] T025 Amend FR-009, FR-013, the edge cases, Key Entities and Assumptions of `spec.md`, add FR-024 and the 003 FR-002 note, add research.md's "Focus", amend the `handoff` section of `contracts/cli.md` and section 5 of the `human-review` skill, and record the tests and one Unverified row in `specs/001-loupe-v1/validation.md`.
- [x] T026 Run `mise run check` until green.
- [x] T027 Live, run by the orchestrator: a handoff from a background Orca tab leaves the view alone and returns `focused: false`; a handoff from the active tab still focuses. Record the outcome in `validation.md` and drop its Unverified row.

## Dependencies

Phase 1 before every later phase: `Host`, `Request`, `Opened`, `Direction`, `TTYWidth` and `failed` are what the rest is written against. Phase 2 needs Phase 1. Phase 3 needs Phases 1 and 2 — `Detect`'s order test exercises `detectHerdr` too, and Herdr's step-key rename in Phase 2 settles the vocabulary `orcaMessage`'s tests mirror. Phase 4 needs Phase 3 (`pane.Detect`, `pane.Direction`, `pane.Host` and `pane.Request` all exist by then); within it, T007 before T008 and T009, and T011 needs T009. Phase 5's T012, T015, T016, T017 and T018 are independent of each other, of Phase 4's code and of T013/T014, which pair (failing test before skill rewrite); T019 depends on knowing what Phases 1 through 4 changed and is filled in last within the phase. Phase 6 needs every prior phase. Phase 7 needs Phase 6 and is run by the human, not this agent.

## Parallel opportunities

T012, T015, T016, T017 and T018 alongside each other and alongside T013/T014; T010 alongside T008 (disjoint files).

## Implementation strategy

Build bottom-up, one file group at a time: the shared seam first (Phase 1), then Herdr rebuilt on it (Phase 2) so the existing host never regresses, then Orca as the second concrete host (Phase 3) — only once both exist does `pane.Detect` compile. Phase 4 wires the CLI to the seam and is where Orca actually becomes reachable as `loupe handoff`. Phase 5's documents can start as soon as the fact they describe is settled, in parallel with later code phases. Phase 6 is the gate; Phase 7 is the owner's live walk in their own Orca session.
